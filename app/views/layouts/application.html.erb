<!DOCTYPE html>
<html>
  <head>
    <title>News Site</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <!--
    <div class="container-fluid py-3 mx-0">
      <div class="d-flex flex-column flex-md-row align-items-center pb-3 mb-4 border-bottom">
        <h5 class="my-0 mr-md-auto fw-bold fs-3"><%= link_to "News Site", home_path, class: "text-decoration-none text-dark" %></h5>
        <nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
          <span class="me-3 py-2 link-body-emphasis text-decoration-none"><%= link_to "Головна сторінка", home_path %></span>
          <span class="me-3 py-2 link-body-emphasis text-decoration-none"><%= link_to "Про нас", about_path %></span>
          <span class="me-3 py-2 link-body-emphasis text-decoration-none"><%= link_to "Додавання посту", new_post_path %></span>
        </nav>
      </div>
    </div>
  -->
  <div class="container-fluid py-3 mx-0">
    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start pb-3 mb-4 border-bottom">
      <h5 class="my-0 mr-md-auto fw-bold fs-3"><%= link_to "News Site", home_path, class: "text-decoration-none text-dark" %></h5>
      <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
        <%= link_to "Головна сторінка", home_path, class: "text-dark me-3", style: "position: relative; top: 3px; left: 16px; text-decoration: none;" %>
        <%= link_to "Про нас", about_path, class: "text-dark me-3", style: "position: relative; top: 3px; left: 16px; text-decoration: none;" %>
        <%= link_to "Додавання посту", new_post_path, class: "text-dark me-3", style: "position: relative; top: 3px; left: 16px; text-decoration: none;" %>
      </ul>
      <div class="text-end">
        <%= render 'include/auth/auth_header' %>
      </div>
    </div>
    <div class="container">
      <%= render 'include/main/notification' %>
    </div>
  </div>
  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>